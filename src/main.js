// Entry point for the theme bundle. Compiled to `assets/theme.js` (+ `assets/theme.css`).
// Per-component behaviour still belongs in {% javascript %} tags — keep this for
// genuinely global code only.
import './styles/tailwind.css';
